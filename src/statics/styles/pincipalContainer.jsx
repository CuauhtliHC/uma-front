import Container from '@mui/material/Container';
import styled from '@mui/system/styled';

const PrincipalContainer = styled(Container)({ marginTop: '73px', padding: 0 });

export { PrincipalContainer };
