import Typography from '@mui/material/Typography';
import styled from '@mui/system/styled';

const MapContainerStyle = {
  height: '30vh',
  width: '100%',
  margin: '5%',
};

const TypographyError = styled(Typography)({ color: 'red', margin: '5%' });

export { MapContainerStyle, TypographyError };
