import Grid from '@mui/material/Grid';
import styled from '@mui/system/styled';
import { TitleGetPackage } from '../getPackage/title.jsx';

const HeaderGridMain = styled(Grid)({ marginLeft: 1, marginRight: 1 });

const HeaderSecondGrid = styled(Grid)({ marginTop: 2.5, marginBottom: 2.5 });

const TitleAddPackageAdmin = styled(TitleGetPackage)({ marginLeft: 1, marginRight: 1 });

export { HeaderGridMain, HeaderSecondGrid, TitleAddPackageAdmin };
